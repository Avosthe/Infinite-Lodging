@page
@model SSDAssignment40.Pages.Audits.EditModel

@{
    ViewData["Title"] = "Edit";
    Layout = "/Pages/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

<h4>AuditRecord</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="AuditRecord.AuditRecordId" />
            <div class="form-group">
                <label asp-for="AuditRecord.AuditActionType" class="control-label"></label>
                <input asp-for="AuditRecord.AuditActionType" class="form-control" />
                <span asp-validation-for="AuditRecord.AuditActionType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AuditRecord.DateTimeStamp" class="control-label"></label>
                <input asp-for="AuditRecord.DateTimeStamp" class="form-control" />
                <span asp-validation-for="AuditRecord.DateTimeStamp" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AuditRecord.IPAddress" class="control-label"></label>
                <input asp-for="AuditRecord.IPAddress" class="form-control" />
                <span asp-validation-for="AuditRecord.IPAddress" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
