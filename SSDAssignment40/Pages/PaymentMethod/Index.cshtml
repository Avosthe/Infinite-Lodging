@page
@model SSDAssignment40.Pages.PaymentMethod.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CreditCard[0].FullName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreditCard[0].CreditCardId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CreditCard) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreditCardId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CreditCardId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CreditCardId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CreditCardId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
